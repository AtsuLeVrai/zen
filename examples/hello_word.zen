func main() -> i32 {
    // Define a constant string message
    const message = "Hello, World!";

    // Print the message using string interpolation
    print(`Message: ${message}`);  // String interpolation syntax

    // Return 0 to indicate successful execution
    return 0;
}