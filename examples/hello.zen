func main() -> i32 {
    let message: string = "Hello, Zen Programming Language!";
    print(message);
    
    const version: string = "v0.1 Alpha";
    print(`Zen Compiler ${version}`);
    
    let result: i32 = add_numbers(10, 20);
    print(`10 + 20 = ${result}`);
    
    return 0;
}

func add_numbers(a: i32, b: i32) -> i32 {
    return a + b;
}

func print(message: string) -> void {
    // Built-in print function (will be implemented in stdlib)
}