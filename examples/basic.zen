func main() -> i32 {
    let message: string = "Hello from Zen!";
    let number: i32 = 42;
    let result: i32 = add_numbers(10, 20);
    return result;
}

func add_numbers(a: i32, b: i32) -> i32 {
    return a + b;
}